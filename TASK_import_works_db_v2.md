# TASK: import current report into works_db_v2

## Source
- Input file: `/home/aboba/.openclaw/workspace/input_report.xlsx`
- Original source: Yandex Disk report provided by Chel

## Goal
Перенести данные из текущего отчета в `works_db_v2`.

## Done
- Развернута новая БД `works_db_v2`.
- Установлены системно `openpyxl` и `psycopg2`.
- Написан первый импортёр: `/home/aboba/.openclaw/workspace/import_report_to_works_db_v2.py`
- Импортированы листы `Уч. 1` ... `Уч. 8`.
- Уже загружено в БД:
  - `construction_sections`: 8
  - `construction_section_versions`: 8
  - `daily_reports`: 8
  - `daily_work_items`: 79
  - `daily_work_item_segments`: 87
  - `objects`: 27
  - `object_segments`: 51
  - `project_work_items`: 93
  - `project_work_item_segments`: 93
  - `work_types`: 61

## User rules (IMPORTANT)
1. Не молчать без причины, держать Chel в курсе.
2. По листу `Общ`:
   - обязательно учитывать принадлежность к строительному участку;
   - строки с диапазонами участков (`1-5`, `5-8`, `1-2` и т.п.) игнорировать;
   - брать только строки с конкретным участком (`1`..`8`);
   - если работы одного конструктива дублируются на одном и том же участке, складывать значения по дням.
3. Не спрашивать лишний раз очевидные следующие шаги.
4. Отписаться, когда задача закончена.

## Current understanding of sheet "Общ"
- Лист `Общ` содержит как агрегирующие строки, так и строки с привязкой к конкретным участкам.
- Конкретные участки встречаются в колонке с участком: `1`, `2`, `3`, `4`, `5`, `6`, `7`, `8`.
- Диапазоны участков (`1-5`, `5-8`, `1-2`) нельзя лить как первичный факт и нужно игнорировать.
- Из `Общ` можно дополнительно вытаскивать строки по конкретным участкам.

## Next step
Сделать второй проход импорта по листу `Общ`:
1. вытащить только строки с конкретным участком;
2. игнорировать диапазоны участков;
3. сгруппировать/суммировать дубли по ключу:
   - участок
   - конструктив/material hint
   - наименование работы
   - дата
4. дозалить/обновить записи в `works_db_v2`.

## Notes
- Были ошибки парсинга границ участков из-за timestamp и обратных диапазонов; исправлены в импортёре.
- Техника и `material_movements` пока не импортированы полноценно.
- Основной незавершенный кусок: лист `Общ`.
